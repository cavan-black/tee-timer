# Copy to mobile/.env and set to a URL your PHONE can reach.
# Local dev: your machine's LAN IP (ipconfig -> IPv4 Address), never localhost.
EXPO_PUBLIC_API_URL=http://192.168.1.42:8000

# Once the API is deployed:
# EXPO_PUBLIC_API_URL=https://tee-timer.vercel.app
